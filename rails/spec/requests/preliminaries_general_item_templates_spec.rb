require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/preliminaries_general_item_templates", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # PreliminariesGeneralItemTemplate. As you add validations to PreliminariesGeneralItemTemplate, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      PreliminariesGeneralItemTemplate.create! valid_attributes
      get preliminaries_general_item_templates_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
      get preliminaries_general_item_template_url(preliminaries_general_item_template)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_preliminaries_general_item_template_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
      get edit_preliminaries_general_item_template_url(preliminaries_general_item_template)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new PreliminariesGeneralItemTemplate" do
        expect {
          post preliminaries_general_item_templates_url, params: { preliminaries_general_item_template: valid_attributes }
        }.to change(PreliminariesGeneralItemTemplate, :count).by(1)
      end

      it "redirects to the created preliminaries_general_item_template" do
        post preliminaries_general_item_templates_url, params: { preliminaries_general_item_template: valid_attributes }
        expect(response).to redirect_to(preliminaries_general_item_template_url(PreliminariesGeneralItemTemplate.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new PreliminariesGeneralItemTemplate" do
        expect {
          post preliminaries_general_item_templates_url, params: { preliminaries_general_item_template: invalid_attributes }
        }.to change(PreliminariesGeneralItemTemplate, :count).by(0)
      end

      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post preliminaries_general_item_templates_url, params: { preliminaries_general_item_template: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_entity)
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested preliminaries_general_item_template" do
        preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
        patch preliminaries_general_item_template_url(preliminaries_general_item_template), params: { preliminaries_general_item_template: new_attributes }
        preliminaries_general_item_template.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the preliminaries_general_item_template" do
        preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
        patch preliminaries_general_item_template_url(preliminaries_general_item_template), params: { preliminaries_general_item_template: new_attributes }
        preliminaries_general_item_template.reload
        expect(response).to redirect_to(preliminaries_general_item_template_url(preliminaries_general_item_template))
      end
    end

    context "with invalid parameters" do
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
        patch preliminaries_general_item_template_url(preliminaries_general_item_template), params: { preliminaries_general_item_template: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_entity)
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested preliminaries_general_item_template" do
      preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
      expect {
        delete preliminaries_general_item_template_url(preliminaries_general_item_template)
      }.to change(PreliminariesGeneralItemTemplate, :count).by(-1)
    end

    it "redirects to the preliminaries_general_item_templates list" do
      preliminaries_general_item_template = PreliminariesGeneralItemTemplate.create! valid_attributes
      delete preliminaries_general_item_template_url(preliminaries_general_item_template)
      expect(response).to redirect_to(preliminaries_general_item_templates_url)
    end
  end
end
