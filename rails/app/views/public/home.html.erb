<div class="min-h-screen bg-gradient-to-br from-blue-50 to-purple-100">
  <!-- Hero Section -->
  <section class="relative py-20 px-4 text-center">
    <div class="absolute top-10 left-10 w-24 h-24 bg-yellow-300 rounded-full opacity-20 animate-pulse"></div>
    <div class="absolute bottom-10 right-10 w-32 h-32 bg-purple-300 rounded-full opacity-20 animate-pulse delay-1000"></div>
    
    <div class="relative z-10 max-w-4xl mx-auto">
      <h1 class="text-5xl md:text-7xl font-bold mb-6 bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-purple-600">
        Creative Playground
      </h1>
      <p class="text-xl text-gray-700 mb-10 max-w-2xl mx-auto">
        Welcome to our interactive Rails app! Explore games, tools, and creative prototypes built with Daisy UI, Tailwind CSS, and Font Awesome.
      </p>
      
      <div class="flex flex-wrap justify-center gap-4 mb-16" data-controller="prototypes--global">
        <button class="btn btn-primary btn-lg gap-2" data-action="click->prototypes--global#confetti">
          <i class="fas fa-party-horn"></i>
          Launch Confetti
        </button>
        <button class="btn btn-secondary btn-lg gap-2" id="theme-toggle">
          <i class="fas fa-palette"></i>
          <span>Toggle Theme</span>
        </button>
        <button class="btn btn-accent btn-lg gap-2" id="random-color">
          <i class="fas fa-random"></i>
          Random Color
        </button>
      </div>
    </div>
  </section>

  <!-- Interactive Cards Grid -->
  <section class="py-12 px-4 max-w-7xl mx-auto">
    <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Explore Our Features</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <!-- Game Cards -->
      <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
        <figure class="px-10 pt-10">
          <div class="w-24 h-24 rounded-full bg-gradient-to-r from-green-400 to-blue-500 flex items-center justify-center">
            <i class="fas fa-gamepad text-4xl text-white"></i>
          </div>
        </figure>
        <div class="card-body items-center text-center">
          <h3 class="card-title text-2xl">Games</h3>
          <p class="text-gray-600">Play Snake and Asteroids games with score tracking</p>
          <div class="card-actions mt-4">
            <a href="/prototypes/snake" class="btn btn-outline btn-success">Play Snake</a>
            <a href="/asteroids_scores" class="btn btn-outline btn-info">Asteroids Scores</a>
          </div>
        </div>
      </div>

      <!-- Productivity Cards -->
      <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
        <figure class="px-10 pt-10">
          <div class="w-24 h-24 rounded-full bg-gradient-to-r from-orange-400 to-red-500 flex items-center justify-center">
            <i class="fas fa-tasks text-4xl text-white"></i>
          </div>
        </figure>
        <div class="card-body items-center text-center">
          <h3 class="card-title text-2xl">Productivity</h3>
          <p class="text-gray-600">Task management, Pomodoro timer, and chat builders</p>
          <div class="card-actions mt-4">
            <a href="/tasks" class="btn btn-outline btn-primary">Tasks</a>
            <a href="/pomodoros" class="btn btn-outline btn-secondary">Pomodoros</a>
          </div>
        </div>
      </div>

      <!-- Creative Tools -->
      <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow duration-300">
        <figure class="px-10 pt-10">
          <div class="w-24 h-24 rounded-full bg-gradient-to-r from-purple-400 to-pink-500 flex items-center justify-center">
            <i class="fas fa-paint-brush text-4xl text-white"></i>
          </div>
        </figure>
        <div class="card-body items-center text-center">
          <h3 class="card-title text-2xl">Creative Tools</h3>
          <p class="text-gray-600">Prototypes and creative builders for experimentation</p>
          <div class="card-actions mt-4">
            <a href="/prototypes" class="btn btn-outline btn-accent">Prototypes</a>
            <a href="/chat_builders" class="btn btn-outline btn-warning">Chat Builders</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Interactive Demo Section -->
  <section class="py-16 px-4 max-w-4xl mx-auto">
    <div class="bg-white rounded-3xl p-8 shadow-2xl border border-gray-200">
      <h2 class="text-3xl font-bold text-center mb-8">Interactive Color Mixer</h2>
      <p class="text-center text-gray-600 mb-8">Adjust the sliders to mix colors and see the result in real-time</p>
      
      <div class="flex flex-col md:flex-row gap-8 items-center">
        <div class="flex-1 space-y-6">
          <div>
            <label class="label">
              <span class="label-text font-semibold">Red: <span id="red-value">128</span></span>
            </label>
            <input type="range" min="0" max="255" value="128" class="range range-error" id="red-slider" />
          </div>
          <div>
            <label class="label">
              <span class="label-text font-semibold">Green: <span id="green-value">128</span></span>
            </label>
            <input type="range" min="0" max="255" value="128" class="range range-success" id="green-slider" />
          </div>
          <div>
            <label class="label">
              <span class="label-text font-semibold">Blue: <span id="blue-value">128</span></span>
            </label>
            <input type="range" min="0" max="255" value="128" class="range range-info" id="blue-slider" />
          </div>
          <button class="btn btn-block btn-outline mt-4" id="copy-color">
            <i class="fas fa-copy mr-2"></i>Copy Color Code
          </button>
        </div>
        
        <div class="flex-1">
          <div class="flex flex-col items-center">
            <div class="w-48 h-48 rounded-2xl shadow-lg mb-6 border border-gray-300" id="color-display"></div>
            <div class="text-center">
              <p class="text-xl font-mono font-bold mb-2" id="color-hex">#808080</p>
              <p class="text-gray-600" id="color-rgb">RGB(128, 128, 128)</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="py-8 text-center text-gray-600 border-t border-gray-300 mt-12">
    <div class="max-w-4xl mx-auto px-4">
      <p class="mb-4">Built with <i class="fas fa-heart text-red-500"></i> using Ruby on Rails 7, Daisy UI, and Tailwind CSS</p>
      <div class="flex justify-center gap-6 text-2xl">
        <a href="#" class="text-blue-600 hover:text-blue-800"><i class="fab fa-github"></i></a>
        <a href="#" class="text-blue-400 hover:text-blue-600"><i class="fab fa-twitter"></i></a>
        <a href="#" class="text-purple-600 hover:text-purple-800"><i class="fab fa-discord"></i></a>
      </div>
    </div>
  </footer>

  <!-- Confetti Canvas (hidden by default) -->
  <canvas id="confettiCanvas" class="absolute top-0 left-0 pointer-events-none w-full h-full" style="display:none; z-index: 50;"></canvas>
</div>

<!-- JavaScript for interactive features -->
<script>
  // Theme Toggle
  document.getElementById('theme-toggle')?.addEventListener('click', function() {
    const html = document.documentElement;
    const currentTheme = html.getAttribute('data-theme');
    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
    html.setAttribute('data-theme', newTheme);
    
    // Update button text
    const icon = this.querySelector('i');
    if (newTheme === 'dark') {
      icon.className = 'fas fa-sun';
      this.querySelector('span').textContent = ' Light Mode';
    } else {
      icon.className = 'fas fa-moon';
      this.querySelector('span').textContent = ' Dark Mode';
    }
  });

  // Random Color Generator
  document.getElementById('random-color')?.addEventListener('click', function() {
    const red = Math.floor(Math.random() * 256);
    const green = Math.floor(Math.random() * 256);
    const blue = Math.floor(Math.random() * 256);
    
    document.getElementById('red-slider').value = red;
    document.getElementById('green-slider').value = green;
    document.getElementById('blue-slider').value = blue;
    
    updateColorDisplay(red, green, blue);
    updateSliderValues(red, green, blue);
  });

  // Color Mixer
  const redSlider = document.getElementById('red-slider');
  const greenSlider = document.getElementById('green-slider');
  const blueSlider = document.getElementById('blue-slider');
  const colorDisplay = document.getElementById('color-display');
  const colorHex = document.getElementById('color-hex');
  const colorRgb = document.getElementById('color-rgb');
  const redValue = document.getElementById('red-value');
  const greenValue = document.getElementById('green-value');
  const blueValue = document.getElementById('blue-value');
  const copyButton = document.getElementById('copy-color');

  function updateSliderValues(r, g, b) {
    redValue.textContent = r;
    greenValue.textContent = g;
    blueValue.textContent = b;
  }

  function rgbToHex(r, g, b) {
    return "#" + ((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1).toUpperCase();
  }

  function updateColorDisplay(r, g, b) {
    const hex = rgbToHex(r, g, b);
    colorDisplay.style.backgroundColor = `rgb(${r}, ${g}, ${b})`;
    colorHex.textContent = hex;
    colorRgb.textContent = `RGB(${r}, ${g}, ${b})`;
  }

  function handleSliderChange() {
    const r = parseInt(redSlider.value);
    const g = parseInt(greenSlider.value);
    const b = parseInt(blueSlider.value);
    
    updateColorDisplay(r, g, b);
    updateSliderValues(r, g, b);
  }

  redSlider.addEventListener('input', handleSliderChange);
  greenSlider.addEventListener('input', handleSliderChange);
  blueSlider.addEventListener('input', handleSliderChange);

  // Copy Color Code
  copyButton.addEventListener('click', function() {
    const hexCode = colorHex.textContent;
    navigator.clipboard.writeText(hexCode).then(() => {
      const originalText = this.innerHTML;
      this.innerHTML = '<i class="fas fa-check mr-2"></i>Copied!';
      setTimeout(() => {
        this.innerHTML = originalText;
      }, 2000);
    });
  });

  // Initialize color display
  updateColorDisplay(128, 128, 128);

  // Confetti - reuse existing confetti logic from prototypes
  // We'll need to check if the controller is available or implement our own
  document.addEventListener('DOMContentLoaded', function() {
    // If confetti button exists but no controller, add basic confetti
    const confettiBtn = document.querySelector('[data-action*="confetti"]');
    if (confettiBtn && !window.confetti) {
      confettiBtn.addEventListener('click', function() {
        // Simple confetti fallback
        createSimpleConfetti();
      });
    }
  });

  function createSimpleConfetti() {
    const canvas = document.getElementById('confettiCanvas');
    if (!canvas) return;
    
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    canvas.style.display = "block";
    const ctx = canvas.getContext("2d");
    
    const confettiCount = 100;
    const confettis = [];
    const colors = ["#FFC700","#FF0000","#2E3191","#41BBC7","#7DF57B","#FF62C6"];
    
    for (let i = 0; i < confettiCount; i++) {
      confettis.push({
        x: Math.random() * canvas.width,
        y: Math.random() * -canvas.height,
        r: 5 + Math.random() * 7,
        d: Math.random() * confettiCount + 10,
        color: colors[Math.floor(Math.random() * colors.length)],
        tilt: Math.floor(Math.random() * 10) - 10,
        tiltAngleIncremental: (Math.random() * 0.07) + .05,
        tiltAngle: 0
      });
    }
    
    let angle = 0;
    let frame = 0;
    
    function drawConfetti() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      confettis.forEach(c => {
        ctx.beginPath();
        ctx.lineWidth = c.r;
        ctx.strokeStyle = c.color;
        ctx.moveTo(c.x + c.tilt + (c.r/3), c.y);
        ctx.lineTo(c.x + c.tilt, c.y + c.tilt + (c.r/5));
        ctx.stroke();
      });
      updateConfetti();
    }
    
    function updateConfetti() {
      angle += 0.01;
      for(let i = 0; i < confettis.length; i++) {
        let c = confettis[i];
        c.y += (Math.cos(angle + c.d) + 3 + c.r/2) / 2;
        c.x += Math.sin(angle);
        c.tiltAngle += c.tiltAngleIncremental;
        c.tilt = Math.sin(c.tiltAngle) * 15;
        
        if (c.y > canvas.height) {
          confettis[i] = {
            ...c,
            x: Math.random() * canvas.width,
            y: -10,
            tilt: Math.floor(Math.random() * 10) - 10
          };
        }
      }
    }
    
    function animate() {
      drawConfetti();
      frame++;
      if(frame < 85) {
        requestAnimationFrame(animate);
      } else {
        canvas.style.display = "none";
        ctx.clearRect(0, 0, canvas.width, canvas.height);
      }
    }
    
    animate();
  }
</script>