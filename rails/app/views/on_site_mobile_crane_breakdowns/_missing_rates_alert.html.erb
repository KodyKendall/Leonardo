<% if missing_rates.any? %>
  <div class="alert alert-error mt-4" role="alert">
    <div class="flex items-start gap-3">
      <i class="fas fa-exclamation-triangle text-lg flex-shrink-0 mt-0.5"></i>
      <div class="flex-1">
        <h3 class="font-bold text-error mb-1">No Cranes Added</h3>
        <p class="text-sm">
          <strong><%= created_count %></strong> crane selection(s) were created, but some crane sizes don't have active rates configured.
        </p>
        <p class="text-sm mt-2">
          <strong>Missing rates for:</strong> <%= missing_rates.join(", ") %>
        </p>
        <p class="text-xs mt-2 text-error-content opacity-75">
          Please configure rates for these crane sizes in the <%= link_to "Crane Rates", new_crane_rate_path, class: "underline font-medium" %> section, or update your breakdown configuration.
        </p>
      </div>
    </div>
  </div>
<% end %>
