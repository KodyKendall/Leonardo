<% content_for :title, "New project rate build up" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New project rate build up</h1>

  <%= render "form", project_rate_build_up: @project_rate_build_up %>

  <%= link_to "Back to project rate build ups", project_rate_build_ups_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
