<%= turbo_stream.update dom_id(@tender_crane_selection) do %>
  <%= render @tender_crane_selection %>
<% end %>

<%# Summary update is handled by the model's after_update_commit broadcast_summary_update callback %>
