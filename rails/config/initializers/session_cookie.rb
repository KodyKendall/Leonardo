# Per-app session cookie configuration (extension point).
#
# The skeleton's default session store is fine for most apps; override here only
# when this app needs custom cookie behavior (e.g. a shared cookie domain or a
# longer expiry). Example:
#
#   Rails.application.config.session_store :cookie_store,
#     key: "_my_app_session",
#     domain: :all,
#     expire_after: 30.days
